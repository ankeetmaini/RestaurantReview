import { StyleSheet } from "react-native";

export const HeaderStyle = StyleSheet.create({
  header: {
    padding: 40,
    fontSize: 30,
    textAlign: "center",
    color: "#06c",
    fontWeight: "300"
  }
});
